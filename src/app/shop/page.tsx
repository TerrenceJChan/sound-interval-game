import ShopPage from "@/page-components/ShopPage";

export const metadata = {
  title: "Shop | Sound Interval Game",
  description: "Spend your hard-earned points to unlock more features.",
};

const Shop = () => {
  return <ShopPage />;
};

export default Shop;
