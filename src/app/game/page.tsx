import GamePage from "@/page-components/GamePage";

export const metadata = {
  title: "Game | Sound Interval Game",
  description: "Train your ears by comparing two sounds.",
};

const Game = () => {
  return <GamePage />;
};

export default Game;
